export const metadata = {
    title: 'Blog || My Funnel ',
    description: 'My Funnel Home page',
  }
export default function OurWorkLayout({ children }) {
    return <section>{children}</section>;
  }